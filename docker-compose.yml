services:
  redis:
    image: redis:7-alpine
    container_name: bitcraft-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - bitcraft_redis_data:/data
    command: redis-server --appendonly yes

volumes:
  bitcraft_redis_data:
